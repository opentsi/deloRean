% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive_seal.R
\name{archive_seal}
\alias{archive_seal}
\title{Seal an Archive by Storing the Current Update Checksum}
\usage{
archive_seal(checksum_input, archive_path = NULL)
}
\arguments{
\item{checksum_input}{An R object to hash, or a 64-character lowercase hex
SHA-256 string.}

\item{archive_path}{Path to the archive/package root directory.
Defaults to the current working directory.}
}
\value{
Invisibly returns the checksum string.
}
\description{
Stamps the archive with a checksum derived from \code{checksum_input} and writes
it to \code{inst/metadata.json}. The JSON is assumed to have already been rendered
from \code{data-raw/metadata.yaml} via \code{\link[=render_metadata]{render_metadata()}} before calling this
function.
}
\details{
If \code{checksum_input} is already a valid 64-character lowercase hex SHA-256
string it is used directly; otherwise the object is hashed with SHA-256 via
\code{\link[digest:digest]{digest::digest()}}.

After sealing, a status summary is printed confirming which workflow stages
are in place.
}
\examples{
\dontrun{
# Hash an R object (e.g. a data frame or publication date)
archive_seal(my_data_frame)

# Pass a pre-computed SHA-256 string
archive_seal("e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855")

# Seal a specific archive
archive_seal(my_data_frame, archive_path = "~/opentsi/ch.kof.barometer")
}
}
